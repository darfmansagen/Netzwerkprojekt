---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 


```{r Gesamtnetzwerk darstellen}
el <- read.csv("https://raw.githubusercontent.com/darfmansagen/Netzwerkprojekt/master/Kopie%20von%20Gesamtnetzwerk%2002.04.%20-%20Edgelist%20(1).csv", header=T, as.is=T, sep = ",")

# Einlesen der Nodelist

nodes <- read.csv("https://raw.githubusercontent.com/darfmansagen/Netzwerkprojekt/master/Kopie%20von%20Gesamtnetzwerk%2002.04.%20-%20Nodelist%20(1).csv", header=T, as.is=T, sep = ",")

# Überprüfen der Daten

head(el)

head(nodes)

# wandelt die Edgelist als Matrix um.

edgematrix <-as.matrix(el)

# ?graph_from_data_frame (erläutert den Befehl genauer)

# erstellt ein igraph-objekt aus der Fusion von Edge- und Nodelist

p <- graph_from_data_frame(d=edgematrix, vertices=nodes, directed=T)


V(p)[(V(p)$sex == 1)]$color <- "red"
V(p)[(V(p)$sex == 2)]$color <- "green"

V(p)[(V(p)$type == 1)]$vertex_shape <- "circle"
V(p)[(V(p)$type == 2)]$vertex_shape <- "square"

plot(p, 

     edge.arrow.size=.3, 

     layout=layout_with_kk, 

     main="Gesamtnetzwerk", 

     sub="Verbindung AfD und IB Deutschland"
     

    
)




```
```{r Dichte}
# Berechnung der Dichte des Gesamtnetzwerks - Anzahl der realisierten Beziehungen

edge_density(p, loops=FALSE)

```
```{r Degree}
degree(p)

```
```{r}
V(p) $degree <- degree(p, degree == 2)?
  Pfaddistanz: 
```

```{r Pfaddistanz}
#Größte Pfaddistanz berechnen
farthest_vertices(p)

```
```{r Pfaddistanz}
#Pfaddistanz zwischen A und B berechnen
distances(p, v = V(p), to = V(p))

#Pfaddistanz zwischen AFD und IBDE berechnen

#Mittlere Pfaddistanz berchenen

mean_distance(p)

#Geringste Pfaddistanz berechnen
```
```{r Betweenness}

#Betweenness berechnen

betweenness(p)
```
```{r Closeness}
#Closeness berechnen
closeness(p)
```

```{r Triaden und Dyaden}

#Triaden 
triad_census(p)




```
```{r}
#gibt an, welcher Knoten an wie vielen Dreiecken beteiligt ist
count_triangles(p)

```

```{r}

#Dyaden
dyad_census(p)


```


```{r Unbenannt}

p2 <- distances(p, v=V(p) [name=="Identitaere Bewegung Deutschland"], to=V(p)
                )
plot(p2)


```

```{r Ego-Netzwerk Kubitschek ersten Grades isolieren}

#Ego-Netzwerke isolieren
ego_size(p)

Kubi <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Goetz Kubitschek", mode ="all")

Kubi

plot(Kubi[[1]])


```
```{r Anzahl der Knoten und Kanten von Kubi ersten Grades}


#Anzahl Kanten berechnen
ecount(Kubi[[1]])

#Anzahl der Knoten berechnen
vcount(Kubi[[1]])

```


```{r Ego-Netzwerk Kubitschek zweiter Grad}

#Netztwerk zweiter Grad


Kubi2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Goetz Kubitschek", mode ="all")


plot(Kubi2[[1]])

```
```{r Knoten und Kanten Kubi zweiten Grades}

#Anzahl Kanten berechnen
ecount(Kubi2[[1]])

#Anzahl der Knoten berechnen
vcount(Kubi2[[1]])

```


```{r Ego-Netzwerke Kubitschek als Vergleich}

par(mfrow=c(1,3))

plot(Kubi[[1]], edge.arrow.size=.3, main="Kubi1 erster Grad")

plot(Kubi2[[1]], edge.arrow.size=.3, main="Kubi2 zweiter Grad")

par(mfrow=c(1,1))


```

```{r Ego-Netzwerk Hans-Thomas Tillschneider erster Grad}

#Ego-Netzwerke isolieren
ego_size(p)

Tillschneider <- make_ego_graph(p, order = 1, nodes = V(p)$name == "HansThomas Tillschneider", mode ="all")

Tillschneider

plot(Tillschneider[[1]])

```
```{r Knoten und Kanten Tillschneider erster Grad}

#Anzahl Kanten berechnen
ecount(Tillschneider[[1]])

#Anzahl der Knoten berechnen
vcount(Tillschneider[[1]])

```


```{r Ego-Netzwerk Hans-Thomas Tillschneider zweiter Grad}

#Ego-Netzwerke isolieren
ego_size(p)

Tillschneider2 <- make_ego_graph(p, order = 2 , nodes = V(p)$name == "HansThomas Tillschneider", mode ="all")

Tillschneider2

plot(Tillschneider2[[1]])

```

```{r Knoten und Kanten Tillschneider zweiter Grad}
#Anzahl Kanten berechnen
ecount(Tillschneider2[[1]])

#Anzahl der Knoten berechnen
vcount(Tillschneider2[[1]])

```
```{r Vergleich Tillschneider}

par(mfrow=c(1,3))

plot(Tillschneider[[1]], edge.arrow.size=.3, main="Tillschneider erster Grad")

plot(Tillschneider2[[1]], edge.arrow.size=.3, main="Tillschneider zweiter Grad")

par(mfrow=c(1,1))

```

```{r Ego-Netzwerk AFD ersten Grades isolieren}

#Ego-Netzwerke isolieren
ego_size(p)

Ego_AFD <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Alternative fuer Deutschland", mode ="all")

Ego_AFD

plot(Ego_AFD[[1]])


```

```{r Anzahl der Knoten und Kanten von AFD ersten Grades}


#Anzahl Kanten berechnen
ecount(Ego_AFD[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_AFD[[1]])

```


```{r Ego-Netzwerk AFD zweiter Grad}

#Netztwerk zweiter Grad


Ego_AFD2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Alternative fuer Deutschland", mode ="all")


plot(Ego_AFD2[[1]])

```

```{r Knoten und Kanten AFD zweiten Grades}

#Anzahl Kanten berechnen
ecount(Ego_AFD2[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_AFD2[[1]])

```

```{r Ego-Netzwerke AFD als Vergleich}

par(mfrow=c(1,3))

plot(Ego_AFD[[1]], edge.arrow.size=.3, main="AFD erster Grad")

plot(Ego_AFD2[[1]], edge.arrow.size=.3, main="AFD2 zweiter Grad")

par(mfrow=c(1,1))


```
```{r Ego-Netzwerk VerlagA ersten Grades isolieren}

#Ego-Netzwerke isolieren 
ego_size(p) 

VerlagA <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Verlag Antaios", mode ="all") 

VerlagA 

plot(VerlagA[[1]]) 


``` 
```{r Anzahl der Knoten und Kanten von VerlagA ersten Grades}


#Anzahl Kanten berechnen 
ecount(VerlagA[[1]]) 

#Anzahl der Knoten berechnen 
vcount(VerlagA[[1]]) 

``` 

```{r Ego-Netzwerk VerlagA zweiter Grad}

#Netztwerk zweiter Grad 


VerlagA2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Verlag Antaios", mode ="all") 


plot(VerlagA2[[1]]) 

``` 
```{r Knoten und Kanten VerlagA zweiten Grades}

#Anzahl Kanten berechnen 
ecount(VerlagA2[[1]]) 

#Anzahl der Knoten berechnen 
vcount(VerlagA2[[1]]) 

``` 
```{r Ego-Netzwerke VerlagA als Vergleich}

par(mfrow=c(1,3)) 

plot(VerlagA[[1]], edge.arrow.size=.3, main="VerlagA erster Grad") 

plot(VerlagA2[[1]], edge.arrow.size=.3, main="VerlagA2 zweiter Grad") 

par(mfrow=c(1,1)) 


``` 

```{r Ego-Netzwerk Sellner erster Grad}

#Netztwerk ersten Grades
Sellner <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Martin Sellner", mode ="all")

plot(Sellner[[1]])

```

```{r Ego-Netzwerk Sellner zweiten Grades}

#Netztwerk ersten Grades
Sellner2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Martin Sellner", mode ="all")

plot(Sellner2[[1]])


```




```{r Knoten und Kanten Sellner zweiten Grades}

#Anzahl Kanten berechnen
ecount(Sellner[[1]])
ecount(Sellner2[[1]])

#Anzahl der Knoten berechnen
vcount(Sellner[[1]])
vcount(Sellner2[[1]])

```





```{r Ego-Netzwerke Kubitschek als Vergleich}

par(mfrow=c(1,3))
plot(Sellner[[1]], edge.arrow.size=.3, main="Sellner erster Grad")
plot(Sellner2[[1]], edge.arrow.size=.3, main="Sellner2 zweiter Grad")
par(mfrow=c(1,1))

```
```{r Ego-Netzwerk IB Deutschland ersten Grades isolieren} 

#Ego-Netzwerke isolieren 
ego_size(p) 

IBD <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Identitaere Bewegung Deutschland", mode ="all") 

IBD 

plot(IBD[[1]]) 


``` 

```{r Anzahl der Knoten und Kanten von IBD ersten Grades} 


#Anzahl Kanten berechnen 
ecount(IBD[[1]]) 

#Anzahl der Knoten berechnen 
vcount(IBD[[1]]) 

``` 

```{r Ego-Netzwerk Identitaere Bewegung Deutschland zweiter Grad} 

#Netztwerk zweiter Grad 


IBD2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Identitaere Bewegung Deutschland", mode ="all") 


plot(IBD2[[1]]) 

``` 

```{r Knoten und Kanten IBD zweiten Grades} 

#Anzahl Kanten berechnen 
ecount(IBD2[[1]]) 

#Anzahl der Knoten berechnen 
vcount(Kubi2[[1]]) 

``` 

```{r Ego-Netzwerke Identitaere Bewegung Deutschland als Vergleich} 

par(mfrow=c(1,3)) 

plot(IBD[[1]], edge.arrow.size=.3, main="IBD1 erster Grad") 

plot(IBD2[[1]], edge.arrow.size=.3, main="IBD2 zweiter Grad") 

par(mfrow=c(1,1)) 


``` 
```{r Ego-Netzwerk Petr Bystron ersten Grades isolieren} 

#Ego-Netzwerke isolieren 
ego_size(p) 

Bystron <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Petr Bystron", mode ="all") 

Bystron 

plot(Bystron[[1]]) 


``` 


```{r Anzahl der Knoten und Kanten von Bystron ersten Grades} 


#Anzahl Kanten berechnen 
ecount(Bystron[[1]]) 

#Anzahl der Knoten berechnen 
vcount(Bystron[[1]]) 

``` 


```{r Ego-Netzwerk Bystron zweiter Grad} 

#Netztwerk zweiter Grad 


Bystron2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Petr Bystron", mode ="all") 


plot(Bystron2[[1]]) 

``` 


```{r Knoten und Kanten Bystron zweiten Grades} 

#Anzahl Kanten berechnen 
ecount(Bystron2[[1]]) 

#Anzahl der Knoten berechnen 
vcount(Bystron2[[1]]) 

``` 


```{r Ego-Netzwerke Bystron als Vergleich} 

par(mfrow=c(1,3)) 

plot(Bystron[[1]], edge.arrow.size=.3, main="Bystron1 erster Grad") 

plot(Bystron2[[1]], edge.arrow.size=.3, main="Bystron2 zweiter Grad") 

par(mfrow=c(1,1)) 


``` 
```{r Ego-Netzwerk Poggenburg ersten Grades isolieren}

#Ego-Netzwerke isolieren
ego_size(p)

Ego_Poggenburg <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Andre Poggenburg", mode ="all")

Ego_Poggenburg

plot(Ego_Poggenburg[[1]])


```

```{r Anzahl der Knoten und Kanten von Poggenburg ersten Grades}


#Anzahl Kanten berechnen
ecount(Ego_Poggenburg[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_Poggenburg[[1]])

```

```{r Ego-Netzwerk Poggenburg zweiter Grad}

#Netztwerk zweiter Grad


Ego_Poggenburg2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Andre Poggenburg", mode ="all")


plot(Ego_Poggenburg2[[1]])

```
```{r Knoten und Kanten Poggenburg zweiten Grades}

#Anzahl Kanten berechnen
ecount(Ego_Poggenburg2[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_Poggenburg2[[1]])

```
```{r Ego-Netzwerke Poggenburg als Vergleich}

par(mfrow=c(1,3))

plot(Ego_Poggenburg[[1]], edge.arrow.size=.3, main="Poggenburg ersten Grades")

plot(Ego_Poggenburg2[[1]], edge.arrow.size=.3, main="Poggenburg zweiter Grad")

par(mfrow=c(1,1))


```

```{r Ego-Netzwerk Koschkar ersten Grades isolieren}

#Ego-Netzwerke isolieren
ego_size(p)

Ego_Koschkar <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Felix Koschkar", mode ="all")

Ego_Koschkar

plot(Ego_Koschkar[[1]])

```

```{r Anzahl der Knoten und Kanten von Koschkar ersten Grades}


#Anzahl Kanten berechnen
ecount(Ego_Koschkar[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_Koschkar[[1]])

```

```{r Ego-Netzwerk Koschkar zweiter Grad}

#Netztwerk zweiter Grad


Ego_Koschkar2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Felix Koschkar", mode ="all")


plot(Ego_Koschkar2[[1]])

```
```{r Knoten und Kanten Koschkar zweiten Grades}

#Anzahl Kanten berechnen
ecount(Ego_Koschkar2[[1]])

#Anzahl der Knoten berechnen
vcount(Ego_Koschkar2[[1]])

```

```{r Ego-Netzwerke Koschkar als Vergleich}

par(mfrow=c(1,3))
plot(Ego_Koschkar[[1]], edge.arrow.size=.3, main="Koschkar ersten Grades")
plot(Ego_Koschkar2[[1]], edge.arrow.size=.3, main="Koschkar zweiter Grad")
par(mfrow=c(1,1))


```
```{r Ego-Netzwerk Lichert erster Grad}

#Netztwerk ersten Grades
Lichert <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Andreas Lichert", mode ="all")
plot(Lichert[[1]])

```

```{r Ego-Netzwerk Lichert zweiten Grades}



#Netztwerk ersten Grades
Lichert2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Andreas Lichert", mode ="all")
plot(Lichert2[[1]])

```

```{r Knoten und Kanten Sellner zweiten Grades}

#Anzahl Kanten berechnen
ecount(Lichert[[1]])
ecount(Lichert2[[1]])

#Anzahl der Knoten berechnen
vcount(Lichert[[1]])
vcount(Lichert2[[1]])

```

```{r Ego-Netzwerke Kubitschek als Vergleich}

par(mfrow=c(1,3))
plot(Lichert[[1]], edge.arrow.size=.3, main="Lichert erster Grad")
plot(Lichert2[[1]], edge.arrow.size=.3, main="Lichert2 zweiter Grad")
par(mfrow=c(1,1))

```
```{r Ego-Netzwerk Dubravkco Mandic ersten Grades isolieren} 

#Ego-Netzwerke isolieren 
ego_size(p) 

Mandic <- make_ego_graph(p, order = 1, nodes = V(p)$name == "Dubravkco Mandic", mode ="all") 

plot(Mandic[[1]]) 


``` 

```{r Anzahl der Knoten und Kanten von Mandic ersten Grades} 


#Anzahl Kanten berechnen 
ecount(Mandic[[1]]) 

#Anzahl der Knoten berechnen 
vcount(Mandic[[1]]) 

``` 

```{r Ego-Netzwerk Mandic zweiter Grad} 

#Netztwerk zweiter Grad 


Mandic2 <- make_ego_graph(p, order = 2, nodes = V(p)$name == "Dubravkco Mandic", mode ="all") 


plot(Mandic2[[1]]) 

``` 

```{r Knoten und Kanten Mandic zweiten Grades} 

#Anzahl Kanten berechnen 
ecount(Mandic2[[1]]) 

#Anzahl der Knoten berechnen 
vcount(Mandic2[[1]]) 

``` 

```{r Ego-Netzwerke Mandic als Vergleich} 

par(mfrow=c(1,3)) 

plot(Mandic[[1]], edge.arrow.size=.3, main="Mandic1 erster Grad") 

plot(Mandic2[[1]], edge.arrow.size=.3, main="Mandic2 zweiter Grad") 

par(mfrow=c(1,1)) 


``` 

